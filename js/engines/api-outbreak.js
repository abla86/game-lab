class ApiOutbreakEngine{constructor(mount,telemetry){this.mount=mount;this.sendTelemetry=telemetry;this.maxTokens=10;this.tokens=10;this.total=0;this.throttled=0;this.interval=null}init(){this.mount.innerHTML='<div class="engine-shell"><div class="engine-top"><strong>API OUTBREAK // TOKEN BUCKET</strong><span id="t">10/10</span></div><div class="engine-actions"><button id="one">REQUEST</button><button id="burst">BURST ×4</button><button id="reset">RESET</button></div><pre id="log" style="margin-top:12px;padding:14px;background:#090611;border:1px solid var(--border);border-radius:9px;min-height:150px">READY</pre></div>';this.interval=setInterval(()=>this.refill(),600);this.mount.querySelector('#one').onclick=()=>this.request(1);this.mount.querySelector('#burst').onclick=()=>this.request(4);this.mount.querySelector('#reset').onclick=()=>this.init();this.sendTelemetry({algorithm:'Token Bucket',capacity:'10',state:'STABLE'})}refill(){if(this.tokens<this.maxTokens){this.tokens++;this.update();}}request(n){const log=this.mount.querySelector('#log');for(let i=0;i<n;i++){this.total++;if(this.tokens>0){this.tokens--;log.textContent+=`\nHTTP 200 · token=${this.tokens}`}else{this.throttled++;log.textContent+='\nHTTP 429 TOO MANY REQUESTS'}}this.update();this.sendTelemetry({tokens:`${this.tokens}/10`,total:this.total,throttled:this.throttled,state:this.tokens?'HEALTHY':'RATE_LIMITED'})}update(){const t=this.mount.querySelector('#t');if(t)t.textContent=`${this.tokens}/10`}destroy(){clearInterval(this.interval);this.interval=null;this.mount.innerHTML=''}}window.ApiOutbreakEngine=ApiOutbreakEngine;